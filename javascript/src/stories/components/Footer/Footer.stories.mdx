import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import AppNavbar from "main/components/Nav/AppNavbar";
import AppFooter from "main/components/Footer/AppFooter";

import { BrowserRouter } from "react-router-dom";
import { createMemoryHistory } from "history";


<Meta title="components/Footer/AppFooter" component={AppFooter} />

# Footer

The `AppFooter` appears at the bottom of every page of the app. It's purpose is to provide context, add
give appropriate credits.

export const Template = (args) => {
  return (
    <BrowserRouter>
      <AppFooter {...args} />
    </BrowserRouter>
  )
}

<Canvas>
  <Story name="Example" args={{ label: 'Example' }}>
    {Template.bind({})}
   </Story>
</Canvas>

