import React from "react";

const AdminSettings = () => {
  return <div>This page will be available to admins to select which quarters are available across the app for searching, statistics, and scheduling. It is currently in progress.</div>;
};

export default AdminSettings;
