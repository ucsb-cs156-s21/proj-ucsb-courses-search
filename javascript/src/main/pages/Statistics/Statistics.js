import React from "react";
import { Jumbotron } from "react-bootstrap";

const Home = () => {
    return (
        <Jumbotron>
            <div className="text-left">
                <h5>This is where the statistics page goes!</h5>
            </div>
        </Jumbotron>
    );
};

export default Home;
